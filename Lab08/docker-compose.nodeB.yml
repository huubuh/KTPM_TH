version: "3.9"

services:
  cart-pu:
    build: ./cart-pu
    container_name: nodeB-cart-pu
    ports:
      - "8082:8082"
    environment:
      - PORT=8082
      - REDIS_HOST=${REDIS_HOST}
      - REDIS_PORT=${REDIS_PORT}